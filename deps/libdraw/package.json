{
  "name": "libdraw",
  "version": "0.2.0",
  "description": "Drawing ASCII picture libraries",
  "install": "install -b libdraw.sh ${PREFIX:-/usr/local}/bin/libdraw",
  "scripts": [ "libdraw.sh" ]
}

